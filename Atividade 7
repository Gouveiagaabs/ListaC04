valores = [0]*10
for indice in range(10):
    num = int(input("Digite um valor inteiro: "))
    valores[indice] = num
maior = max(valores)
ind = valores.index(maior)
print("O maior elemento do vetor é " + str(maior) + " e está localizado no índice " + str(ind))
